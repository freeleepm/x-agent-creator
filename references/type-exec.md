# exec 类型配置

## 权限配置

| 权限 | 设置 |
|:-----|:-----|
| exec | ✅ 可用（security=full） |
| fs | ✅ 读写权限 |
| runtime | ✅ 可用 |
| browser | ❌ 不可用 |
| nodes | ❌ 不可用 |

## 适用场景

- 写代码/开发/执行命令
- 运行脚本/编译
- 系统管理操作
- 需要执行外部命令的任务

## 使用注意

- 执行命令前确认命令安全性
- 优先使用 `trash` 而非 `rm`
- 删除/权限修改前必须先询问确认
