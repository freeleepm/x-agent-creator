# read-only 类型配置

## 权限配置

| 权限 | 设置 |
|:-----|:-----|
| exec | ❌ 不可用 |
| fs | ✅ 只读权限 |
| runtime | ✅ 可用 |
| browser | ❌ 不可用 |
| nodes | ❌ 不可用 |

## 适用场景

- 看/分析/只读操作
- 数据分析
- 文档查看
- 不需要执行命令的任务

## 使用注意

- 只能读取文件，不能写入/修改
- 不能执行任何命令
- 需要写入时需要明确用户授权
